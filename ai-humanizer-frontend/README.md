# StealthSuite - Frontend

This is the user interface for the StealthSuite application, built with **Next.js 14**, **Tailwind CSS**, and **Framer Motion**.

## 🛠️ Setup

1.  **Install Dependencies:**
    ```bash
    npm install
    ```

2.  **Run Development Server:**
    ```bash
    npm run dev
    ```

3.  **Open Localhost:**
    Navigate to [http://localhost:3000](http://localhost:3000).

## 🧩 Environment Variables

If you deploy this, ensure your backend URL is configured. locally it defaults to `localhost:8000`.

## 🎨 Key Pages
* `/agents` - Mission control for AI agents.
* `/whisper` - Live audio visualizer.
* `/humanize` - Text obfuscation tool.